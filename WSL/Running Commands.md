# Running Commands

```powershell
wsl -e <command>                    # Run a single command and exit
wsl -- <command> arg1 arg2          # Run a command with arguments
```

## Examples

```powershell
wsl -- htop
```
